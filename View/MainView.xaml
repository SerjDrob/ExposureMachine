<Window x:Class="ExposureMachine.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExposureMachine.ViewModel"
        mc:Ignorable="d"
        Title="Установка совмещения" Height="450" Width="800"  WindowState="Maximized">
    <Window.InputBindings>
        <KeyBinding Command="{Binding PushCmd}" CommandParameter="{x:Static local:Buttons.AlignmentMask}" Key="F2"/>
    </Window.InputBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="Black" BorderThickness="3">
            <Image />
        </Border>
        <Border Background="Black" BorderThickness="3" Grid.Column="1">
            <Image Grid.Column="1"/>
        </Border>
        <UniformGrid Grid.Row="1" Grid.ColumnSpan="2" Rows="2" Columns="5">
            <UniformGrid.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="2"/>
                </Style>
            </UniformGrid.Resources>
            <Button  Content="Юстировка ФШ" Command="{Binding PushCmd}" CommandParameter="{x:Static local:Buttons.AlignmentMask}" />
            <Button Content="Фиксация ФШ" Command="{Binding PushCmd}" CommandParameter="{x:Static local:Buttons.FixingMask}"/>
            <Button Content="Фиксация рамки ФШ" Command="{Binding PushCmd}" CommandParameter="{x:Static local:Buttons.FixingFrame}"/>
            <Button Content="Фиксация подложки" Command="{Binding PushCmd}" CommandParameter="{x:Static local:Buttons.FixingSubstrate}"/>
            <Button Content="Столик шаровая опора" Command="{Binding PushCmd}" CommandParameter="{x:Static local:Buttons.BallSupport}"/>
            <Button Content="Подъём столика" Command="{Binding PushCmd}" CommandParameter="{x:Static local:Buttons.LiftingTable}"/>
            <Button>
                <TextBlock TextWrapping="Wrap" Text="Фиксация подъёма столика"/>
            </Button>
            <Button Content="Зазор"/>
            <Button Content="Экспонирование"/>
        </UniformGrid>
    </Grid>
</Window>
